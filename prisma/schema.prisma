// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// =============================================================
// SCHEMA METADATA - Metadados mínimos dos schemas
// =============================================================

// Metadados básicos dos schemas criados (apenas informação auxiliar)
model SchemaMetadata {
  id                  String   @id @default(cuid())
  schemaId            String   // "user_profile" (legível)
  schemaIdBytes32     String   // "0x75736572..." (bytes32 para contrato)
  schemaName          String   // "User Profile Schema" (descritivo)
  channelName         String   // "coffee_channel"
  channelNameBytes32  String   // "coffee_channel"
  transactionHash     String   @unique // "0xabc..." (hash da tx de criação)
  walletAddress       String   // Endereço que criou o schema
  createdAt           DateTime @default(now())
  
  // Garantir unicidade por canal (mesmo que no contrato)
  @@unique([schemaId, channelName])
  @@map("schema_metadata")
}

// =============================================================
// PROCESS METADATA - Metadados mínimos dos processos
// =============================================================

// Metadados básicos dos processos criados (apenas informação auxiliar)
model ProcessMetadata {
  id                    String   @id @default(cuid())
  processId             String   // "coffee_harvest" (legível)
  processIdBytes32      String   // "0x636f666665..." (bytes32 para contrato)
  natureId              String   // "arabica_beans" (legível)
  natureIdBytes32       String   // "0x617261626963..." (bytes32 para contrato)
  stageId               String   // "drying_stage" (legível)
  stageIdBytes32        String   // "0x647279696e67..." (bytes32 para contrato)
  channelName           String   // "coffee_channel"
  channelNameBytes32    String   // "0x636f666665..." (bytes32 para contrato)
  transactionHash       String   @unique // "0xabc..." (hash da tx de criação)
  walletAddress         String   // Endereço que criou o processo
  createdAt             DateTime @default(now())
  
  // Garantir unicidade por canal (mesmo que no contrato)
  @@unique([processId, natureId, stageId, channelName])
  @@map("process_metadata")
}



// Model para armazenar endereços de contratos registrados
//model ContractAddress {
//  id              String   @id @default(cuid())
//  contractName    String   @unique // Nome do contrato (ex: "ACCESS_CHANNEL_MANAGER")
//  contractHash    String   @unique // Hash bytes32 do nome do contrato
//  address         String   // Endereço atual do contrato
//  isActive        Boolean  @default(true)
  
  // Metadados
//  createdAt       DateTime @default(now())
//  updatedAt       DateTime @updatedAt
  
  // Relacionamentos
//  updates         ContractAddressUpdate[]
  
//  @@map("contract_addresses")
//}

// Histórico de atualizações de endereços
//model ContractAddressUpdate {
//  id              String   @id @default(cuid())
//  contractId      String
//  oldAddress      String?  // Endereço anterior (null na primeira criação)
//  newAddress      String   // Novo endereço
//  updatedBy       String   // Endereço de quem fez a atualização
//  transactionHash String   @unique
//  blockNumber     BigInt
//  blockTimestamp  DateTime
  
  // Metadados
//  createdAt       DateTime @default(now())
  
  // Relacionamentos
//  contract        ContractAddress @relation(fields: [contractId], references: [id], onDelete: Cascade)
  
//  @@map("contract_address_updates")
//}